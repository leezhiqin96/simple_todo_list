doctype html
html
  head
    title= title
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width,initial-scale=1, minimum-scale=1")
    script(type='text/javascript').
      const csrfToken= '!{csrfToken}';
  body
    div#root(class="login-page")
    script(src='/dist/login.js')
